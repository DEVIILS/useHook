import { BlockState } from "./pages/BlockState";

export const App = () => {
    // const [count, setCount] = useState(0);
    // console.log("Count:", count);

    return (
        <>
            <BlockState />
        </>
    );
};
